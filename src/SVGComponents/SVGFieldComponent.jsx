const SVGFieldComponent = ({ width, height, backgroundColor }) => {
  return (
    <svg
      style={{
        width,
        height,
        backgroundColor,
      }}
    ></svg>
  );
};

export { App };
